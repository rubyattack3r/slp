sub greet(name) {
  println("Hello " . name);
}
greet("World");
1 + 2
$a = 5
$a * 4
4 * 4
println(4)
println("4")
$a = 5
$a * 2
"hello"
%(1 => "a", 2 => @(1, 2, 3))
true
3
%(1 => "a", 2 => @(1, 2, 3))
test = %(1 => "a", 2 => @(1, 2, 3))
test
test = 1
test
test = 1; println(test);
test = %(1 => "a", 2 => @(1, 2, 3)); test;
$myfunc = { println("test"); }; [$myfunc];
sub make_counter { $c = 0; return { $c = $c + 1; return $c; }; } $c1 = make_counter(); println([$c1]); println([$c1]); $c2 = make_counter(); println([$c2]);
test = sub { println("works"); }; test();
sub make_counter { $c = 0; return sub { $c = $c + 1; return $c; }; } $c1 = make_counter(); println([$c1]); println([$c1]); $c2 = make_counter(); println([$c2]);
sub make_counter { return sub { $1 = $1 + 1; return $1; }; } $c1 = make_counter(0); println([$c1]); println([$c1]); $c2 = make_counter(0); println([$c2]);
