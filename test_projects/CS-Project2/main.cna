sub readbof {
    local('$handle');
    $handle = openf(script_resource("modules/bof2.o"));
    println("Loaded BOF 2 from " . script_resource("modules/bof2.o"));
}

# Collision with Project 1
alias project1_cmd {
    $callback = &readbof;
    [$callback];
}
